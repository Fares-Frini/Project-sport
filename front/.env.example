# ============================================================
# Frontend Angular environment variables — Vercel
# ============================================================
# On Vercel: Project > Settings > Environment Variables
#
# set-env.js reads API_URL at build time and injects it into
# src/environments/environment.prod.ts before ng build runs.
#
# For local dev, the fallback in set-env.js is used automatically:
#   http://localhost:5292/api
# ============================================================

# --- Backend API URL (required on Vercel) ---
# Full base URL of your Render backend, no trailing slash
# Example: https://parapharma-api.onrender.com/api
API_URL=https://your-backend.onrender.com/api
