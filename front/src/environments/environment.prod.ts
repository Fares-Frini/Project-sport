// Ce fichier est généré dynamiquement avant le build par set-env.js
// NE PAS éditer manuellement — définir la variable d'environnement API_URL dans Vercel
export const environment = {
  production: true,
  apiUrl: 'PLACEHOLDER_API_URL'
};
