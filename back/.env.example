# ============================================================
# Variables d'environnement — Backend .NET (Render)
# ============================================================
# Sur Render, définissez ces variables dans :
#   Service > Environment > Add Environment Variable
#
# ASP.NET Core mappe automatiquement :
#   ConnectionStrings__OltpConnection  →  ConnectionStrings:OltpConnection
#   Jwt__Key                           →  Jwt:Key
# (double underscore __ = séparateur de section)
# ============================================================

# --- Base de données OLTP (PostgreSQL sur Render) ---
# Récupérez cette URL depuis : Render > votre DB > Info > Internal Database URL
# Format : Host=HOST;Port=5432;Database=DB_NAME;Username=USER;Password=PASS;
ConnectionStrings__OltpConnection=Host=dpg-xxx-a.oregon-postgres.render.com;Port=5432;Database=parapharm_oltp;Username=parapharm_user;Password=VOTRE_MOT_DE_PASSE;

# --- Base de données DWH (PostgreSQL sur Render) ---
ConnectionStrings__ExamDWHConnection=Host=dpg-xxx-a.oregon-postgres.render.com;Port=5432;Database=parapharm_dwh;Username=parapharm_user;Password=VOTRE_MOT_DE_PASSE;

# --- JWT ---
# Clé secrète : minimum 32 caractères, générez-la aléatoirement
Jwt__Key=CHANGEZ_MOI_cle_secrete_minimum_32_caracteres_aleatoires
Jwt__Issuer=ParaPharmaAPI
Jwt__Audience=ParaPharmaClient

# --- CORS ---
# URL(s) du frontend Vercel (séparées par virgule si plusieurs)
# Exemple : https://parapharmacy.vercel.app,https://www.mon-domaine.com
AllowedOrigins=https://votre-frontend.vercel.app

# --- ASP.NET Core ---
ASPNETCORE_ENVIRONMENT=Production
