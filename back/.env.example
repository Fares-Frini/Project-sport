# ============================================================
# Backend .NET environment variables — Render
# ============================================================
# On Render: Service > Environment > Add Environment Variable
#
# ASP.NET Core automatically maps double-underscore __ as
# section separators:
#   ConnectionStrings__OltpConnection  →  ConnectionStrings:OltpConnection
#   Jwt__Key                           →  Jwt:Key
# ============================================================

# --- ASP.NET Core ---
ASPNETCORE_ENVIRONMENT=Production

# --- OLTP Database (PostgreSQL on Render) ---
# Get from: Render > your DB > Info > Internal Database URL
# Format: Host=HOST;Port=5432;Database=DB_NAME;Username=USER;Password=PASS
ConnectionStrings__OltpConnection=Host=YOUR_DB_HOST;Port=5432;Database=YOUR_DB_NAME;Username=YOUR_DB_USER;Password=YOUR_DB_PASSWORD

# --- DWH Database (PostgreSQL on Render) ---
# Can point to the same DB as OLTP if not using a separate DWH instance
ConnectionStrings__ExamDWHConnection=Host=YOUR_DWH_HOST;Port=5432;Database=YOUR_DWH_NAME;Username=YOUR_DWH_USER;Password=YOUR_DWH_PASSWORD

# --- JWT ---
# Secret key: minimum 32 random characters
# Generate with: openssl rand -base64 32
Jwt__Key=CHANGE_ME_minimum_32_random_characters_here
Jwt__Issuer=ParaPharmaAPI
Jwt__Audience=ParaPharmaClient

